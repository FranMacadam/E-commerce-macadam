import "./Header.scss";

const CartWidget = () => {
    return (
        <div>
            <span className="material-icons" id='shoppingCart'>
                shopping_cart
            </span>  
        </div>
    );
}

export default CartWidget;
